# 🚀 Инструкция по развертыванию на GitHub Pages

## Шаг 1: Подготовка репозитория

1. Создайте новый репозиторий на GitHub с именем `almirsiterecode` (или любое другое имя)

2. Инициализируйте git в вашем проекте:
```bash
git init
git add .
git commit -m "Initial commit: Futuristic Portfolio"
```

3. Подключите удаленный репозиторий:
```bash
git remote add origin https://github.com/YOUR_USERNAME/almirsiterecode.git
git branch -M main
git push -u origin main
```

## Шаг 2: Настройка base path

**ВАЖНО:** Убедитесь, что в `vite.config.js` указан правильный путь:

```js
base: '/almirsiterecode/', // Замените на имя вашего репозитория
```

Если ваш репозиторий называется по-другому, измените это значение.

## Шаг 3: Установка зависимостей

```bash
npm install
```

## Шаг 4: Деплой

Выполните команду деплоя:

```bash
npm run deploy
```

Эта команда:
1. Соберет проект (`npm run build`)
2. Создаст ветку `gh-pages` с содержимым папки `dist`
3. Отправит изменения на GitHub

## Шаг 5: Активация GitHub Pages

1. Перейдите в настройки вашего репозитория на GitHub
2. Откройте раздел **"Pages"** в меню слева
3. В разделе **"Source"** выберите ветку **"gh-pages"**
4. Выберите папку **"/ (root)"**
5. Нажмите **"Save"**

## Шаг 6: Ожидание публикации

GitHub Pages обычно публикует сайт в течение 1-2 минут. Ваш сайт будет доступен по адресу:

```
https://YOUR_USERNAME.github.io/almirsiterecode/
```

## 🔄 Обновление сайта

После любых изменений в коде:

1. Внесите изменения
2. Закоммитьте изменения:
```bash
git add .
git commit -m "Update portfolio"
git push
```

3. Задеплойте обновления:
```bash
npm run deploy
```

## ⚠️ Важные замечания

- **Base path**: Всегда проверяйте, что `base` в `vite.config.js` соответствует имени вашего репозитория
- **Пути к ресурсам**: Все пути к изображениям и другим ресурсам должны быть относительными
- **Кэш**: После деплоя может потребоваться очистить кэш браузера (Ctrl+Shift+R)

## 🐛 Решение проблем

### Сайт не загружается / показывает 404

1. Проверьте, что `base` в `vite.config.js` правильный
2. Убедитесь, что ветка `gh-pages` создана и содержит файлы
3. Проверьте настройки GitHub Pages в репозитории

### Стили не применяются

1. Убедитесь, что сборка прошла успешно
2. Проверьте консоль браузера на ошибки
3. Попробуйте очистить кэш браузера

### Изображения не загружаются

1. Убедитесь, что изображения находятся в папке `public` или используют абсолютные URL
2. Проверьте пути к изображениям в коде

## 📞 Поддержка

Если возникли проблемы, проверьте:
- [Документацию Vite](https://vitejs.dev/)
- [Документацию GitHub Pages](https://docs.github.com/en/pages)
- [Документацию gh-pages](https://github.com/tschaub/gh-pages)
