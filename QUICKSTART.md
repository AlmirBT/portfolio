# ⚡ Быстрый старт

## 🚀 Запуск проекта локально

1. **Установите зависимости:**
```bash
npm install
```

2. **Запустите dev сервер:**
```bash
npm run dev
```

3. **Откройте браузер:**
Перейдите на `http://localhost:5173`

## 📝 Персонализация

### Измените имя и информацию

1. **Hero секция** (`src/components/Hero.jsx`):
   - Замените "ALMIR" на ваше имя
   - Измените теглайн "Creative Developer & Digital Artist"
   - Обновите описание

2. **About секция** (`src/components/About.jsx`):
   - Замените текст о себе
   - Обновите карточки с вашими ценностями

3. **Skills секция** (`src/components/Skills.jsx`):
   - Добавьте/удалите навыки в массиве `skills`
   - Измените уровни (проценты)
   - Добавьте иконки из `react-icons`

4. **Projects секция** (`src/components/Projects.jsx`):
   - Замените проекты в массиве `projects`
   - Добавьте реальные ссылки на GitHub и Live демо
   - Замените изображения на свои

5. **Contact секция** (`src/components/Contact.jsx`):
   - Обновите социальные ссылки
   - Измените контактную информацию
   - Настройте форму (для реальной отправки нужен backend)

6. **Navigation** (`src/components/Navigation.jsx`):
   - Измените логотип/имя в навигации

### Измените цвета

Отредактируйте `tailwind.config.js`:

```js
colors: {
  neon: {
    cyan: '#00ffff',    // Ваш цвет
    pink: '#ff00ff',   // Ваш цвет
    purple: '#9d00ff', // Ваш цвет
  }
}
```

### Измените шрифты

В `index.html` замените Google Fonts на свои:

```html
<link href="https://fonts.googleapis.com/css2?family=YourFont:wght@400;700&display=swap" rel="stylesheet">
```

Затем обновите `tailwind.config.js`:

```js
fontFamily: {
  sans: ['YourFont', 'sans-serif'],
}
```

## 🎨 Кастомизация анимаций

Все анимации используют Framer Motion. Вы можете настроить их в каждом компоненте:

- `initial` - начальное состояние
- `animate` - анимация при появлении
- `whileHover` - анимация при наведении
- `transition` - параметры перехода

## 📦 Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

## 🌐 Деплой на GitHub Pages

См. подробную инструкцию в `DEPLOY.md`

```bash
npm run deploy
```

## 🐛 Решение проблем

### Порт занят
Если порт 5173 занят, Vite автоматически предложит другой порт.

### Ошибки при установке
Убедитесь, что у вас установлен Node.js версии 16 или выше:
```bash
node --version
```

### Стили не применяются
Убедитесь, что TailwindCSS правильно настроен:
```bash
npm run dev
```

Проверьте консоль браузера на ошибки.

## 📚 Полезные ссылки

- [React документация](https://react.dev/)
- [Vite документация](https://vitejs.dev/)
- [TailwindCSS документация](https://tailwindcss.com/)
- [Framer Motion документация](https://www.framer.com/motion/)
- [React Icons](https://react-icons.github.io/react-icons/)

---

**Готово! Начните создавать свой уникальный портфолио! 🎉**
